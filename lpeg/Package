# configuration properties

V.lpegSourcePackage ?= lpeg-0.12

# package description

conf   = .config
deps   = crank crank-c crank-lua lua lpeg=$(call v.,lpegSourcePackage)
result = out/$v
make   = make V='$v'
clean  = make V='$v' clean
